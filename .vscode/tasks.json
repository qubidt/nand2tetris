{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run test file",
            "type": "shell",
            "command": "./tools/HardwareSimulator.sh ${file}",
            "windows": {
                "command": "./tools/HardwareSimulator.bat ${file}"
            },
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}